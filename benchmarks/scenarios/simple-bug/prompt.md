# Simple bug

A unit test fails because a loop uses `<=` instead of `<` on a zero-based index.

Reproduce the failure, then apply the smallest change that makes the test pass.
Do not refactor neighboring code.
